# ============================================================
# 实验参数覆盖文件
# 继承 base_config.yaml，只写本次实验需要改动的参数
# 配合 config_schema.apply_overrides 使用
#
# 使用示例：
#   from strategy.config_schema import load_config, apply_overrides
#   import yaml
#   base = load_config()
#   with open("experiment_overrides.yaml") as f:
#       overrides = yaml.safe_load(f)
#   config = apply_overrides(base, overrides)
# ============================================================

# ---- 实验名称：缠论参数敏感性测试 ----
tech:
  bi_min_kbar: 6                # 增大最小笔K线数（更保守）
  noise:
    er_period: 24               # 增大ER周期（更慢响应）
    er_low_percentile: 0.25     # 降低噪声阈值（更多信号通过）

# ---- 单参数实验：融合分数线 ----
# fusion:
#   buy_score_min: 65           # 提高买入门槛
#   strong_buy_score: 90        # 高分跟随门槛

# ---- 单参数实验：仓位管理 ----
# position:
#   single_stock_max_pct: 0.06  # 降低单只上限
#   daily_risk_budget_pct: 0.02 # 降低日预算
