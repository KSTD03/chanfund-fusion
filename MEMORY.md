# MEMORY.md — 量化策略开发默认工作模式

## 核心架构
- **Qlib** 负责：选股、因子挖掘、AI信号生成、策略回测（历史绩效评估）
- **vn.py** 负责：买卖指令执行、动态风控、实盘账户对接、订单管理

## 数据使用规则
- 生成任何策略代码前，必须先确认数据可用性，明确数据起止时间、频率、字段
- 若涉及新增数据字段，先检查本地是否存在；若不存在，给出补全建议（如 Tushare 增量更新）

## 策略研发标准流程
1. **因子构思** → 用自然语言描述选股逻辑
2. **Qlib 实现** → 生成可在 Qlib 中运行的因子表达式及训练/回测脚本
3. **信号输出** → Qlib 每日输出目标股票池及仓位权重（格式为 CSV 或 DataFrame）
4. **vn.py 对接** → 生成独立 Python 脚本，读取 Qlib 信号文件，调用 vn.py 的 CtaTemplate 或 Strategy 接口生成交易指令
5. **回测验证** → 使用 Qlib 完成历史回测，使用 vn.py 回测模式验证执行逻辑（滑点、手续费、涨跌停限制）
6. **实盘部署** → 确认风控参数后，生成可直接运行的 vn.py 实盘启动脚本（含日志、监控、异常处理）

## 代码输出要求
- 所有脚本必须是可独立运行的 Python 文件，含必要注释和配置文件示例
- 优先使用 Qlib 的 Alpha158 / Alpha360 等内置因子集，必要时自定义表达式
- vn.py 策略需继承 CtaTemplate，实现 on_tick / on_bar / on_order / on_trade 核心方法
- 提供 requirements.txt 及环境安装说明

## 记录时间
2026-05-03 22:20 — 老板定稿 Qlib + vn.py 双引擎工作模式

## 回测时间划分标准（老板定稿 2026-05-04）

## ChanFund Fusion 版本流与关键发现（2026-05-09 更新）

### 版本演进
```
v1.4 (基线, 夏普3.44) → v2.0 (信号因子化, -0.16) → v2.1 (scores, 0.18) 
  → v2.2 (env乘数, 0.18) → v2.3 (MA分类+波动率, 0.11) → v2.4 (真实scores, -0.12)
```

### 关键数据Bug修复
- **双前缀bug**：parquet数据code已有`sh.`前缀，lambda `f"sh.{c}"` 导致双重前缀
  修复后覆盖率从0%→100%
- **环境分类器**：ER效率比率>0.6太严格(98%震荡)，改用MA20/60交叉+波动率85%分位硬保护

### 当前未解决问题
- scores.parquet评分模型可能有问题（真实评分接入后夏普反降）
- 回测框架代码重复问题严重，需要统一引擎

## 关于实际回测框架（实际使用vs理论）
- 实际回测**不走Qlib引擎**，使用独立Python脚本手动日循环
- 策略引擎`chanfund_fusion/`虽继承Qlib BaseStrategy，但仅用于代码组织
- 缠论增量状态机是100%自研，性能约5000只×4000天=25分钟
- 数据源混合使用：Qlib Parquet + Tushare + 自建CSV
全量数据范围：2005-2026（22年），三段拆分防过拟合：
1. **训练集**：2005-2015（11年）→ 写策略、初选参数
2. **验证集**：2016-2018（3年）→ 调参筛模型、规避过拟合
3. **测试集**：2019-2026（8年）→ 样本外终极校验，贴近实盘

## scores.parquet 评分模型质量分析（2026-05-10 定稿）

### 核心发现
- **score-PnL 相关性 r=0.036, p=0.46** → 统计上无预测能力
- 评分模型是截面排名质量因子（ROE/毛利率/负债率/FCF/PE/PB等），非缠论信号预测器
- 高分票（茅台92/药明康德90）和低分票（绩差股3-6分）的缠论交易胜率无明显差异

### 为什么 v2.4 用了真实 scores 反而更差
- v2.3: 默认50分 → 无减仓 → 夏普 0.11
- v2.4: 真实分 → score<40减仓(~23%交易被减半) → 夏普 -0.12
- 被减仓的 low-score 交易仍有 57-62% 胜率（正期望），减仓是削正期望仓位
- 这与 v2.1 (夏普 0.18) 的做法不同：v2.1 是**入场前过滤**(<30拒绝)+共振加权

### 建议方案
- **短期**：改 post-entry 减仓为 pre-trade 过滤（score<30 拒绝）
- **中期**：重训评分模型，目标函数改为预测缠论信号后收益
