# 心跳策略

## 配置状态（2026-05-05）
- ✅ 已设置 cron：`0 */3 * * *` Asia/Shanghai（0/3/6/9/12/15/18/21点）
- ✅ 任务ID：19f1838b-e5a4-4079-84db-cfb6be913926
- ✅ 类型：systemEvent（main session）

## 安静模式
- 所有心跳系统事件必须用 `NO_REPLY` 静默处理
- 禁止输出 `HEARTBEAT_OK` 等任何文字
- 禁止输出状态码、调试信息

## 任务模式
- 有任务时在此处添加对应指令
- 无任务时保持静默

## 规则
- HEARTBEAT_OK 禁止输出
- 状态码禁止输出
- 调试信息禁止输出
- 无任务时只回复 `NO_REPLY`

## 后台进程监控规则（2026-05-10 更新）
- 启动长时间任务后，必须立即创建轮询 check 命令
- 每 5-10 分钟检查进程状态（CPU/内存/运行时间）
- 每 15 分钟检查系统可用内存（free -h），防止内存泄漏
- 任务完成后/失败后，及时清理残留进程
- 查询进程：`ps -o pid,%cpu,%mem,rss,etime -p <PID>`
- 查询系统内存：`free -h | head -2`
- 发现僵尸进程 → 立即 kill -9
